# 2026-07-27 工作日志

## 任务：将证书管理项目 Java 后端转为 Python

### 项目概况
- 路径：`/Users/gengjiaxing/ai-code-product/zhengshu_roo_code0618_gemini_all_story`
- 原后端：Spring Boot (Java) 位于 `backend/`，DDL 风格分层（interfaces/application/infrastructure/domain）
- 前端：Vue3 + Vite，调用后端 `/api/certificates`
- 数据库：MySQL `cert_claude_code0624_roo`，表 `certificates`（root/root，端口 3306）

### 原后端测试发现的问题
- Java 后端启动在 8081 端口，context-path `/api`
- Bug1：分页接口 `total/pages` 恒为 0（未配置 MyBatis-Plus 分页拦截器）
- Bug2：POST 创建返回 500（`created_at/modified_at` 非空约束失败，审计字段未自动填充）
- 字段为驼峰命名（expiryDate / createdAt / modifiedAt），DB 列为蛇形

### Python 后端实现（backend_python/）
- 技术栈：FastAPI 0.115 + SQLAlchemy 2.0 + PyMySQL + Pydantic 2 + Uvicorn
- 分层与 Java 版对齐：interfaces/controller、application/service、infrastructure/repository、models
- 修正了上述两个 Bug：仓储层自动填充审计时间、真实计算分页 total/pages
- API 完全兼容：`/api/certificates` CRUD + `/api/health`，统一信封 `{code,message,data}`
- 字段驼峰别名映射（schemas.py 的 CamelModel + alias_generator）

### 测试结果
- pytest 10 个用例全通过（基于 SQLite 内存库，无需 MySQL）
  - 关键点：模型主键用 `Integer` 而非 `BigInteger`（SQLite 下 BigInteger 不生成 AUTOINCREMENT）
  - conftest 每个测试独立内存 engine + dependency_overrides 注入 get_db
- 联调（连真实 MySQL，8082 端口）：列表/创建/详情/更新/删除/409 唯一约束 均正常
- 分页正确返回 total=9, pages=2（验证 Java 版 bug 已修复）

### Git
- 分支 `main-jingjian-python`，新增 commit `2be9711`
- 已推送到 `origin/main-jingjian-python`（新远程分支）
- PR 链接：https://github.com/ITcoder007/zhengshu_roo_code0618_gemini_all_story/pull/new/main-jingjian-python
